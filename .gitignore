__pycache__/
*.pyc
.env
venv/
.venv/

# 데이터 (API로 수집한 원본, 용량 크고 재현 가능하므로 커밋하지 않음)
data/raw/
data/processed/
*.sqlite
*.db

# 벡터 DB 로컬 스토리지
chroma/
.chroma/

# 뉴스 API 자체 일일 호출량 카운터 (로컬 상태, 커밋 불필요)
src/data/.news_api_usage.json

# DART corp_code 조회 캐시 (30MB짜리 zip에서 파싱한 결과, 로컬 캐시라 커밋 불필요)
src/data/.dart_corp_code_cache.json

# 실험 입출력 로그 (baseline/agent 실행 기록, 로컬 확인용 - 커밋 불필요)
logs/

# 사람이 직접 훑어보기 위한 데이터 export (재생성 가능, 커밋 불필요)
data/*_export.*

.ipynb_checkpoints/
.DS_Store
